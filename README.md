//We will follow Role-Bases-Architecture:
- src/
    index.js/ // serve
    models/
    controllers/
    middlewares/
    services/
    utils/    // Helper function:
    configure/  //Inside it we keep database configuration and the configuration that we need or required
    repository/
-test/ [Later]